export default function OutreachPage() {
  return (
    <div className="space-y-2">
      <h1 className="text-xl font-semibold">Outreach</h1>
      <p className="max-w-xl text-sm text-[var(--text-muted)]">
        The CRM pipeline (sales statuses, Kanban board, per-lead outreach log) lands in Phase 7, once discovery and
        scoring against a real provider are validated. The <code>outreach</code> table already exists and lead sales
        status is tracked today from each lead&apos;s detail page.
      </p>
    </div>
  );
}
